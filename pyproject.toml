[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fe-be-alignment-analysis"
version = "0.1.0"
description = "Finetune ERP monorepo"
readme = "README.md"
requires-python = ">=3.11"

[tool.black]
line-length = 88
